<section
  class="min-h-screen flex items-center justify-center px-6 bg-gray-50 dark:bg-gray-900"
>
  <div
    class="max-w-7xl w-full mx-auto grid md:grid-cols-2 gap-12 lg:gap-20 items-center"
  >
    <!-- Left Content: Text -->
    <div class="space-y-6 order-2 md:order-1">
      <!-- Greeting -->
      <p class="text-blue-600 dark:text-blue-400 text-lg font-medium">
        Hello! I'm
      </p>

      <!-- Name -->
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold leading-tight">
        <span class="text-gray-900 dark:text-white block">Carla</span>
        <span class="text-blue-600 dark:text-blue-400 block"
          >Clemente Figueroa</span
        >
      </h1>

      <!-- Role/Title -->
      <p
        class="text-xl md:text-2xl text-gray-600 dark:text-gray-400 font-medium"
      >
        Full-Stack Developer
      </p>

      <!-- Description -->
      <p
        class="text-gray-600 dark:text-gray-400 text-base md:text-lg leading-relaxed max-w-lg"
      >
        Building elegant solutions to complex problems with modern technologies.
      </p>
    </div>

    <!-- Right Content: Photo with decorative elements -->
    <div class="relative flex justify-center order-1 md:order-2">
      <div class="relative">
        <!-- Decorative dots - Top Left (Orange) -->
        <div
          class="absolute -left-6 top-8 md:-left-8 md:top-12 grid grid-cols-3 gap-2 opacity-70 z-0"
        >
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
          <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
        </div>

        <!-- Decorative dots - Bottom Right (Gray) -->
        <div
          class="absolute -right-6 bottom-8 md:-right-8 md:bottom-12 grid grid-cols-3 gap-2 opacity-70 z-0"
        >
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
          <div class="w-2 h-2 bg-gray-600 dark:bg-gray-400 rounded-full"></div>
        </div>

        <!-- Background gradient shape (behind photo) -->
        <div
          class="absolute inset-0 bg-gradient-to-br from-purple-500/60 to-gray-400 dark:from-purple-700/40 dark:to-gray-700/40 rounded-3xl transform rotate-6 z-0"
        >
        </div>

        <!-- Main image container with gradient overlay -->
        <div
          class="relative bg-gradient-to-br from-purple-300/60 to-gray-400 dark:from-purple-800/60 dark:to-gray-600/60 rounded-3xl overflow-hidden w-72 h-96 md:w-80 md:h-[450px] lg:w-96 lg:h-[500px] shadow-2xl z-10"
        >
          <img
            src="/public/carla-clemente.png"
            alt="Carla Clemente Figueroa"
            class="absolute -bottom-6 w-full h-[120%] object-cover"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animación sutil para los elementos decorativos */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  /* Aplicar animación a los puntos decorativos */
  .absolute.grid {
    animation: float 3s ease-in-out infinite;
  }

  .absolute.grid:nth-child(2) {
    animation-delay: 1s;
  }
</style>
